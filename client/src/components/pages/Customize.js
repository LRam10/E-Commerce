import React from 'react';
import {useExpiration} from '../../utils/useExpiration';

const Customize= ()=> {
    useExpiration();
    return (
        <div>
            <h1>customize</h1>
        </div>
    )
}

export default Customize
